spring:
  data:
    redis:
      host: localhost
      port: 6379
      password:             # ???? ??? ???
      timeout: 3000ms       # ?? ????

      # Lettuce ??? ? ??
      lettuce:
        pool:
          max-active: 8     # ??? ?? ?? ??? ?? ??? ?
          max-idle: 8       # ?? ??? ??? ?? ??? ?
          min-idle: 0       # ?? ??? ??? ?? ??? ?
          max-wait: -1ms    # ??? ??? ??? ? ?? ?? (-1 = ???)